import MTC/lab9/lab9;

main()
{
	test1 = "x / (x + 1) - 1 / (x - 1) + (x + 2) / ((x+2)*(x-1))";
	println("test: " + test1);
	result1 = parsic(algebraGrammar(),test1,subDivPegActions);
	println("e2r test: " + e2s(e2r(result1,"x")));
	println("e2rs test: " + e2rs(result1,"x"));
	quit(0);
}