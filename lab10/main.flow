import MTC/lab10/lab10;
import MTC/lab8/lab8;

main()
{
    test = "x * x * a * (c + 1) + x * a * (c + 1) * x";
    result = parsic(algebraGrammar(),test,subDivPegActions);
    simplified = simplify(result,defIdentities);
    println(simplification2s(result, simplified));
    quit(0);
}